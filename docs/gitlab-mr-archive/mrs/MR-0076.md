# GitLab MR !76 - feat: 아이 프로필 생성 화면 구현

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/76 |
| State | merged |
| Source | feature/S14P31A404-235-child-profile-edit |
| Target | develop |
| Author | soshyzx123 |
| Assignees | soshyzx123 |
| Labels | feature, mobile |
| Created | 2026-05-05T22:08:56.000+09:00 |
| Updated | 2026-05-06T09:38:58.187+09:00 |
| Merged | 2026-05-06T09:38:57.182+09:00 |
| Closed |  |
| Head SHA | 349923d831bd8594943a69e185e2d04a094da304 |
| Migration Classification | github-source-branch |
| GitHub Source Branch | True |
| GitHub Recovered Branch | False |
| Diff Archive | [../diffs/MR-0076.diff](../diffs/MR-0076.diff) |

## Description

## 요약
- 아이 프로필 생성 화면을 추가하고 생성 후 목록이 갱신되도록 연결

## 작업 배경
- 관련 이슈: #S14P31A404-235
- 참고 문서/기획: APP-A 작업 목록, 아이 프로필 선택 화면(S14P31A404-234)

## 주요 변경 사항
- 아이 이름/나이 입력 기반 아이 프로필 생성 화면 구현
- `ChildProfileRepository`에 생성 인터페이스를 추가하고 Fake Repository에 메모리 저장 흐름 구현
- 생성 성공 시 `SavedStateHandle` 플래그로 목록 화면을 자동 갱신
- 저장 중 중복 클릭 방지 및 입력 검증/오류 상태 처리
- 변경하지 않은 것 / 스코프 아웃: 실제 백엔드 API 연동, 삭제/비활성 처리, 아바타/이미지 업로드

## 스크린샷/로그
- UI 변경 사항: 아이 프로필 생성 화면, 생성 후 목록 갱신 흐름
- 현재 백엔드 API 미구현으로 Fake Repository 메모리 저장소 기반 동작

## 테스트 결과 (선택사항)
- [x] `ktlintCheck` 통과
- [x] `detekt` 통과
- [x] `lintDebug` 통과
- [x] `testDebugUnitTest` 통과
- [x] `assembleDebug` 통과

## 작성자 체크리스트
- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [x] 로컬 빌드/런 정상
- [x] 관련 문서/주석/README 업데이트(필요 시)
- [ ] 이슈/Jira 상태 업데이트(In Review/Code Review)

## 메모
- 아이 프로필 API가 아직 없어 `ChildProfileRepository` 인터페이스 확장과 Fake 구현체 기반으로 선구현했습니다.
- 생성된 프로필은 앱 실행 중 Fake Repository 메모리 리스트에 저장되어 목록 복귀 후 확인할 수 있습니다.
- 생성 성공 후 `SavedStateHandle`의 `child_profile_changed` 플래그를 통해 선택 화면 목록을 다시 조회합니다.
- 실제 API 연동 시 Remote Repository 구현체로 교체하고 Hilt 바인딩을 변경하는 방향을 권장합니다.
- 나이 검증은 임시로 0~18세 범위를 적용했으며, 백엔드 명세 확정 시 정책 동기화가 필요합니다.

Closes S14P31A404-235

## Notes

### soshyzx123 at 2026-05-05T22:08:56.136+09:00 (system)

```text
requested review from @hh001204
```

### soshyzx123 at 2026-05-05T22:08:56.159+09:00 (system)

```text
assigned to @soshyzx123
```

### hh001204 at 2026-05-06T09:31:47.031+09:00 (system)

```text
deleted the `feature/S14P31A404-234-child-profile-select` branch. This merge request now targets the `develop` branch
```

### hh001204 at 2026-05-06T09:38:38.012+09:00 (comment)

```text
**P3: 키보드가 입력 화면 하단을 가릴 수 있습니다**

아이 프로필 생성 화면의 본문 Column이 `fillMaxSize`와 `padding`만 사용하고 있어서, 작은 화면에서 키보드가 올라오면 나이 입력칸이나 저장 버튼이 가려질 수 있습니다.

이름/나이 입력 화면인 만큼 `verticalScroll`과 `imePadding`을 같이 적용해두면 실제 기기에서 더 안정적으로 사용할 수 있을 것 같습니다.
```

### hh001204 at 2026-05-06T09:38:38.284+09:00 (comment)

```text
전체적으로 아이 프로필 생성 흐름이 깔끔하게 잘 들어간 것 같습니다.

입력 검증, 저장 중 중복 클릭 방지, `Fake Repository` 저장, 생성 후 목록 자동 갱신까지 자연스럽게 연결되어 있어서 현재 스코프 기준으로는 충분히 좋아 보입니다. 남긴 코멘트는 실제 기기에서 키보드가 올라왔을 때의 사용성 보강 정도라, 큰 구조 문제는 아닌 것 같습니다.

고생 많으셨습니다! 👍👍
```

### hh001204 at 2026-05-06T09:38:38.412+09:00 (system)

```text
approved this merge request
```

### hh001204 at 2026-05-06T09:38:44.718+09:00 (system)

```text
resolved all threads
```

### hh001204 at 2026-05-06T09:38:57.332+09:00 (system)

```text
mentioned in commit 91fbaf91eb1ab73485eca554e9450676ac633a0e
```


# GitLab MR !185 - feat: 아이 프로필 avatarKey API 구현

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/185 |
| State | merged |
| Source | feature/S14P31A404-252-child-profile-avatar-key |
| Target | develop |
| Author | ellastin1023 |
| Assignees | ellastin1023 |
| Labels | backend, feature, fix |
| Created | 2026-05-17T16:29:11.776+09:00 |
| Updated | 2026-05-17T18:11:10.841+09:00 |
| Merged | 2026-05-17T18:11:06.604+09:00 |
| Closed |  |
| Head SHA | 698d28a00c49708019cdb274c2bb403b68abe0d7 |
| Migration Classification | github-recovered-branch |
| GitHub Source Branch | False |
| GitHub Recovered Branch | True |
| Diff Archive | [../diffs/MR-0185.diff](../diffs/MR-0185.diff) |

## Description

## 요약

`- 아이 프로필에 avatarKey 필드를 추가하여 모바일에서 선택한 프로필 이미지를 key 기반으로 저장 및 조회할 수 있도록 개선`

---

## 작업 배경

* 관련 이슈: S14P31A404-252
* 참고 문서/기획: 모바일 팀 avatarKey 필드 추가 요청

---

## 주요 변경 사항

* 아이 프로필 Entity에 `avatarKey` 컬럼 추가 및 DB migration 반영
* 생성/수정 Request DTO에 `avatarKey` 필드 추가
* 상세/목록/생성/수정 Response DTO에 `avatarKey` 필드 추가
* avatarKey 유효성 검증 및 기본값(`purple_diamond`) 처리 로직 추가
* 기존 데이터 및 미입력 케이스에 대해 기본값 반환 처리

---

## 스크린샷/로그

---

## 테스트 결과 (선택사항)

* [x] API 응답/로그 캡처 (Swagger 테스트 완료)
* [ ] 단위 테스트 작성

---

## 작성자 체크리스트

* [x] Secret/Env/개인정보가 포함되지 않음
* [x] 불필요한 로그/디버그 코드 제거
* [x] 로컬 빌드/런 정상
* [x] 관련 문서/주석/README 업데이트(필요 시)
* [x] 이슈/Jira 상태 업데이트(In Review/Code Review)

---

## 메모

* avatarKey는 이미지 파일을 저장하지 않고 문자열 key만 DB에 저장하며, 실제 이미지는 모바일 앱 내부 리소스를 사용하여 표시
* 허용된 avatarKey 목록에 대해서만 저장 가능하도록 검증 로직 적용
* 기존 데이터 및 avatarKey 미입력 시 기본값(`purple_diamond`)으로 처리

---

Closes S14P31A404-252

## Notes

### ellastin1023 at 2026-05-17T16:29:11.914+09:00 (system)

```text
requested review from @hh001204
```

### ellastin1023 at 2026-05-17T16:29:11.937+09:00 (system)

```text
assigned to @ellastin1023
```

### hh001204 at 2026-05-17T16:45:49.961+09:00 (comment)

```text
전체적으로 요청드린 `avatarKey` 반영은 잘 들어간 것 같습니다.

확인한 내용:
- 아이 프로필 생성/수정 Request에 `avatarKey` 추가
- 상세/목록 Response에 `avatarKey` 추가
- `child_profiles.avatar_key` 컬럼 추가
- 기존 데이터 기본값 `purple_diamond` 처리
- 실제 이미지 파일 저장 없이 문자열 key만 저장
- 허용된 avatar key 목록 검증 로직 추가

다만 한 가지 확인 부탁드립니다.
현재 잘못된 `avatarKey`가 들어오면 `VALIDATION_INVALID_INPUT`으로 처리되는 것으로 보이는데, 생성 API 문서의 에러 코드 목록에는 해당 코드가 빠져 있는 것 같습니다.
수정 API 문서에는 포함되어 있으니, 생성 API 문서에도 동일하게 추가해 주시면 Swagger/API 문서 기준으로도 더 일관될 것 같습니다.

테스트 코드는 이번 확인 범위에서 제외하고 봤습니다.
```

### hh001204 at 2026-05-17T16:45:50.162+09:00 (system)

```text
approved this merge request
```

### hh001204 at 2026-05-17T18:11:06.900+09:00 (system)

```text
mentioned in commit f960564d99fc62862f9d5cea3213df1c864c73db
```


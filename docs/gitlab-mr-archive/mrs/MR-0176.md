# GitLab MR !176 - feat: 양방향 소통 대화 세션 저장 API 연동

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/176 |
| State | merged |
| Source | feature/S14P31A404-328-comms-session-api |
| Target | develop |
| Author | soshyzx123 |
| Assignees | soshyzx123 |
| Labels | feature, mobile |
| Created | 2026-05-15T13:36:51.524+09:00 |
| Updated | 2026-05-16T16:21:05.468+09:00 |
| Merged | 2026-05-16T16:21:03.666+09:00 |
| Closed |  |
| Head SHA | 27c5dc3eb7ee0143583c7e269adf41dfa2c748b4 |
| Migration Classification | github-recovered-branch |
| GitHub Source Branch | False |
| GitHub Recovered Branch | True |
| Diff Archive | [../diffs/MR-0176.diff](../diffs/MR-0176.diff) |

## Description

## 요약
- 양방향 소통 화면에서 대화 세션을 생성하고 STT/TTS 요청에 sessionId를 전달하도록 연동


## 작업 배경
- 관련 이슈: #S14P31A404-328
- 참고 문서/기획: 백엔드 Comms 대화 세션 API 연동 가이드

## 주요 변경 사항
- `POST /api/v1/comms/sessions`, `PATCH /api/v1/comms/sessions/{sessionId}/end` 연동 추가
- 대화 시작/종료 시 active child 기준 세션 생성 및 종료 처리
- 수어→TTS, 음성→STT 요청에 nullable/optional `sessionId` 전달
- 세션 저장 실패 시 실시간 소통 기능은 유지되도록 실패 격리
- 변경하지 않은 것 / 스코프 아웃: 대화 기록 조회 UI, AI 리포트 화면, 백엔드 코드 수정

## 스크린샷/로그
- API 연동 변경: 실제 기기 QA 시 `ConversationViewModel`, `CommsSessionRepository` Logcat 캡처 첨부 예정

##  테스트 결과 (선택사항)
- [x] 단위 테스트 작성 / 통과 여부
  - `cd mobile && .\gradlew.bat ktlintCheck detekt lintDebug testDebugUnitTest assembleDebug --no-daemon`
  - `BUILD SUCCESSFUL`
- [ ] API 응답/로그 캡처 (스크린샷 첨부)


## 작성자 체크리스트
- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [x] 로컬 빌드/런 정상
- [ ] 관련 문서/주석/README 업데이트(필요 시)
- [ ] 이슈/Jira 상태 업데이트(In Review/Code Review)

## 메모
- 세션 생성/종료 실패가 발생해도 기존 실시간 STT/TTS 기능은 계속 동작하도록 처리했습니다.
- 로그인 상태에서는 Auth Retrofit을 통해 translation API에도 토큰이 전달되도록 변경했습니다.
- AI 리포트/대화 기록 조회 화면은 백엔드 분석 API 확정 후 별도 티켓에서 진행 예정입니다.


Closes S14P31A404-328

## Notes

### soshyzx123 at 2026-05-15T13:36:51.653+09:00 (system)

```text
requested review from @ellastin1023
```

### soshyzx123 at 2026-05-15T13:36:51.676+09:00 (system)

```text
assigned to @soshyzx123
```

### nej0720 at 2026-05-15T14:33:43.198+09:00 (comment)

```text
\[P2\] 세션 생성 완료 전에 번역/음성 인식이 시작되어 첫 대화가 저장되지 않을 수 있습니다

`startSession()`에서 `startCommsSession()`을 비동기로 실행한 뒤 바로 수어 인식과 STT 녹음을 시작하고 있습니다. 이 상태에서는 `createSession()` 응답이 오기 전까지 `commsSessionId`가 null이라, 사용자가 세션 시작 직후 바로 말하거나 수어 입력을 완료하면 translation 요청이 sessionId 없이 전송되어 해당 메시지가 대화 기록에 저장되지 않을 수 있습니다.

대화 기록 저장이 이 기능의 핵심이라면 세션 생성이 성공한 뒤 인식/녹음을 시작하거나, 세션 생성 전 발생한 첫 메시지를 sessionId 준비 후 저장할 수 있도록 보완하는 게 안전해 보입니다!
```

### nej0720 at 2026-05-15T14:34:28.697+09:00 (comment)

```text
\[P3\] 온디바이스/로컬 STT 모드에서도 빈 대화 세션이 생성될 수 있습니다

현재 `startSession()`은 번역 모드와 관계없이 항상 comms 세션 생성을 시도합니다. 그런데 실제 메시지 저장은 서버 번역 요청이나 cloud STT 요청에 `sessionId`를 실어 보낼 때만 이루어져서, ON_DEVICE 모드나 로컬 STT로만 대화한 경우에는 세션만 생성되고 메시지는 저장되지 않을 수 있습니다.

의도적으로 서버 처리된 대화만 저장하는 정책이라면 괜찮지만, 그렇다면 온디바이스/로컬 모드에서는 세션 생성을 생략하거나, 반대로 모든 대화를 저장해야 한다면 로컬 처리 결과도 저장할 수 있는 흐름이 필요해 보입니다.
```

### nej0720 at 2026-05-15T14:36:05.896+09:00 (comment)

```text
대화 세션 API 연동 구조는 전반적으로 잘 연결된 것 같습니다. 세션 생성 후 translation 요청에 sessionId를 전달하고, 화면 종료 시 세션 종료 API를 호출하는 흐름도 확인했습니다.

다만 세션 생성이 비동기로 진행되는 동안 첫 대화가 sessionId 없이 전송될 수 있는 부분과, 온디바이스/로컬 모드에서 빈 세션이 생길 수 있는 부분은 정책에 맞게 한 번 확인해보면 좋을 것 같습니다. 

고생하셨습니다! :smile: :thumbsup: :thumbsup: :thumbsup: 

![CuteBunnyCarrotAnimatedGIF.gif](/uploads/137bab093a6071239b1a054193caa260/CuteBunnyCarrotAnimatedGIF.gif){width=138 height=138}
```

### soshyzx123 at 2026-05-15T14:44:27.722+09:00 (comment)

```text
확인했습니다. 두 의견 모두 유효한 지적으로 보입니다. :grin: 

현재 `startCommsSession()`이 비동기로 실행된 직후 바로 수어 인식/STT 녹음이 시작되기 때문에, 세션 생성 완료 전에 첫 translation 요청이 나가면 `sessionId` 없이 전송되어 첫 메시지가 저장되지 않을 수 있습니다.

또한 온디바이스/로컬 STT 모드에서는 서버 translation API를 호출하지 않아 메시지가 저장되지 않는데, 현재 구조에서는 세션만 생성되어 빈 세션이 남을 수 있습니다.

수정 방향은 아래처럼 잡겠습니다.

- 서버 저장이 가능한 모드(`SERVER`, `AUTO && online`)에서만 comms session 생성 시도
- 세션 생성 성공/실패가 확정된 뒤 인식/녹음 시작
- 세션 생성 실패 시에는 기존처럼 저장 없이 소통 기능은 계속 동작
- 온디바이스/로컬 모드에서는 현재 백엔드 저장 경로가 없으므로 세션 생성을 생략

이 방향으로 보완하겠습니다!!! :skull:
```

### soshyzx123 at 2026-05-15T14:44:28.010+09:00 (system)

```text
left review comments
```

### soshyzx123 at 2026-05-15T15:06:04.223+09:00 (system)

```text
changed this line in [version 2 of the diff](/s14-final/S14P31A404/-/merge_requests/176/diffs?diff_id=2054947&start_sha=487e944b90da3f64c07d00ea847656a2a91cd576#e4df534f5c08dd7b8c1dfbb02a7838a329799399_735_747)
```

### soshyzx123 at 2026-05-15T15:06:04.421+09:00 (system)

```text
added 1 commit

<ul><li>f3d57606 - fix: 대화 세션 생성 후 소통 입력 시작</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/176/diffs?diff_id=2054947&start_sha=487e944b90da3f64c07d00ea847656a2a91cd576)
```

### soshyzx123 at 2026-05-15T15:06:39.867+09:00 (comment)

```text
꼼꼼히 리뷰 해주셔서 미리 보완할 수 있었습니다! 감사합니다!!!

![ChainsawManCsmGIF](/uploads/5c6592e94123be92d54c4df249f6d44c/ChainsawManCsmGIF.gif){width=498 height=371}
```

### soshyzx123 at 2026-05-15T15:06:40.002+09:00 (system)

```text
left review comments
```

### soshyzx123 at 2026-05-15T15:07:08.512+09:00 (system)

```text
resolved all threads
```

### nej0720 at 2026-05-15T15:15:48.449+09:00 (system)

```text
requested review from @nej0720 and removed review request for @ellastin1023
```

### nej0720 at 2026-05-15T15:23:27.578+09:00 (comment)

```text
\[P3\] 세션 중 서버 저장 가능 상태로 바뀌어도 comms session이 생성되지 않을 수 있습니다

초기 시작 시점에는 `shouldCreateCommsSession()` 기준으로 서버 저장 가능한 모드에서만 세션을 만들도록 잘 수정된 것 같습니다. 다만 세션이 이미 시작된 뒤에 `ON_DEVICE → SERVER`로 모드를 바꾸거나, `AUTO` 모드에서 offline으로 시작했다가 online으로 전환되는 경우에는 현재 로직이 STT만 재시작하고 comms session은 새로 생성하지 않는 것 같습니다.

이 경우 이후부터는 서버 translation/cloud STT를 사용하더라도 `commsSessionId`가 계속 null이라 대화가 저장되지 않을 수 있습니다. 세션 중 모드/네트워크 변경으로 서버 저장 가능 상태가 된 경우에도 comms session을 준비할지 정책 확인 후 보완하면 좋을 것 같습니다!
```

### nej0720 at 2026-05-15T15:23:57.410+09:00 (comment)

```text
확인했습니다! 초기 세션 시작 시 세션 생성 성공/실패가 확정된 뒤 인식/STT를 시작하도록 수정된 것 확인했습니다. 또한 시작 시점에 ON_DEVICE 또는 AUTO offline이면 comms session 생성을 생략하는 것도 확인했습니다.

다만 세션 진행 중 모드나 네트워크 상태가 바뀌어 서버 저장 가능 상태가 되는 경우에는 comms session이 새로 생성되지 않을 수 있어 보여서, 이 케이스만 추가로 확인 부탁드립니다.
```

### soshyzx123 at 2026-05-15T16:25:29.965+09:00 (system)

```text
added 1 commit

<ul><li>27c5dc3e - fix: 서버 저장 가능 전환 시 대화 세션 보장</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/176/diffs?diff_id=2055401&start_sha=f3d57606549aa586df90df40c630f5c2f5400380)
```

### soshyzx123 at 2026-05-15T16:25:50.015+09:00 (comment)

```text
반영했습니다. :grin: 

- 서버 저장 가능한 상태에서 cloud sign-to-speech / cloud STT 요청을 보내기 직전에 comms session을 보장하도록 변경했습니다.
- 세션 시작 시점에 ON_DEVICE 또는 AUTO 오프라인으로 시작했더라도, 이후 SERVER 전환/온라인 복구 후 첫 서버 요청 전에 sessionId를 생성해 함께 전달합니다.
- sign-to-speech와 STT가 동시에 세션 생성을 시도해도 중복 세션이 생기지 않도록 Mutex로 보호했습니다.
- 세션 생성 실패 시에는 기존처럼 sessionId 없이 실시간 소통은 계속 동작합니다.

![MaugaRaiseEyebrowsGIF](/uploads/a51231c21c62f3c5414d7e5de36c823b/MaugaRaiseEyebrowsGIF.gif){width=236 height=236}
```

### soshyzx123 at 2026-05-15T16:25:50.144+09:00 (system)

```text
left review comments
```

### soshyzx123 at 2026-05-16T16:20:55.975+09:00 (system)

```text
resolved all threads
```

### soshyzx123 at 2026-05-16T16:21:04.166+09:00 (system)

```text
mentioned in commit 065f960fe0fed541449f8773283913c8edef1da8
```


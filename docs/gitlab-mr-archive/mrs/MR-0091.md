# GitLab MR !91 - feat: 유저 관련 DB 스키마 수정

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/91 |
| State | merged |
| Source | feature/S14P31A404-315-user-schema-update |
| Target | develop |
| Author | nej0720 |
| Assignees | nej0720 |
| Labels | backend, feature |
| Created | 2026-05-06T18:53:03.198+09:00 |
| Updated | 2026-05-07T09:24:29.570+09:00 |
| Merged | 2026-05-07T09:21:40.028+09:00 |
| Closed |  |
| Head SHA | e8d37316a5c4fbce6cd096916d0e60036114f683 |
| Migration Classification | metadata-or-patch-only |
| GitHub Source Branch | False |
| GitHub Recovered Branch | False |
| Diff Archive | [../diffs/MR-0091.diff](../diffs/MR-0091.diff) |

## Description

## 요약

- 아이별 학습 기록 관리를 위해 유저 관련 DB 스키마를 수정했습니다.

## 작업 배경

- 관련 이슈: Closes S14P31A404-315
- 참고 문서/기획: 
- 보호자 계정과 아이 프로필을 분리하고, 퀴즈 결과를 아이 기준으로 저장하기 위한 선행 스키마 작업입니다.

## 주요 변경 사항

- `users` 테이블에 보호자 이름 `name` 컬럼 추가
- `child_profiles` 테이블 추가
- `social_accounts` 테이블 추가
- `quiz_sessions.user_id` 참조 제거 후 `child_profile_id` 참조로 변경

## 변경하지 않은 것 / 스코프 아웃

- JPA 엔티티, Repository, Service, API 구현은 후속 작업에서 진행합니다.

## 스크린샷/로그

- UI 변경 없음
- API 변경 없음

## 테스트 결과

- 마이그레이션 파일 추가 작업으로 별도 단위 테스트는 작성하지 않았습니다.
- `git diff --check` 확인 완료

## 작성자 체크리스트

- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [ ] 로컬 빌드/런 정상
- [ ] 관련 문서/주석/README 업데이트(필요 시)
- [ ] 이슈/Jira 상태 업데이트(In Review/Code Review)

## 메모

- 현재 `quiz_sessions`에 데이터가 없는 전제로 `child_profile_id`를 `NOT NULL`로 추가했습니다.
- 후속 작업에서 `User`, `ChildProfile`, `SocialAccount` 엔티티 및 관련 API 구현이 필요합니다.

Closes S14P31A404-315

## Notes

### nej0720 at 2026-05-06T18:53:03.332+09:00 (system)

```text
requested review from @ellastin1023
```

### nej0720 at 2026-05-06T18:53:03.360+09:00 (system)

```text
assigned to @nej0720
```

### soshyzx123 at 2026-05-07T08:21:46.486+09:00 (comment)

```text
P1: 기존 quiz_sessions 데이터가 있는 환경에서 마이그레이션이 실패할 수 있어 보입니다. :skull: 
 

현재 흐름은 `quiz_sessions`에 `child_profile_id` 컬럼을 추가한 뒤, 기존 `user_id`를 삭제하고 바로 `child_profile_id SET NOT NULL`을 적용하고 있습니다.

문제는 기존 `quiz_sessions` 행이 한 건이라도 있으면 새로 추가된 `child_profile_id` 값이 모두 NULL이라는 점입니다. 이 상태에서 `SET NOT NULL`을 실행하면 Flyway가 `column contains null values` 류의 오류로 중단될 수 있습니다.

기존 데이터가 없음을 확실히 보장할 수 있다면 그대로 진행해도 될거같슴다.

스키마 방향 자체는 좋아 보여요, 다만 이 부분은 배포 시 DB 마이그레이션 실패 가능성이 있어서 확인이 필요해 보입니다!! :grin: :grin: :grin:
```

### ellastin1023 at 2026-05-07T09:21:37.717+09:00 (system)

```text
approved this merge request
```

### ellastin1023 at 2026-05-07T09:21:40.279+09:00 (system)

```text
mentioned in commit dc7b75b7ab3ab2deadb9ff54abf4332dbdbad311
```

### nej0720 at 2026-05-07T09:24:29.544+09:00 (comment)

```text
`quiz_sessions` 에 데이터 없는 거 확인하고 진행했습니다! 

리뷰 감사합니다\~!
```


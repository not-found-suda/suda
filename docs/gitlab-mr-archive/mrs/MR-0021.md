# GitLab MR !21 - fix: 모바일 공통 아키텍처 MR 피드백 반영 및 오류 수정

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/21 |
| State | merged |
| Source | feature/S14P31A404-136-review-feedbacks |
| Target | develop |
| Author | soshyzx123 |
| Assignees | soshyzx123 |
| Labels | fix, mobile |
| Created | 2026-04-25T21:27:52.798+09:00 |
| Updated | 2026-04-27T10:47:46.605+09:00 |
| Merged | 2026-04-27T10:47:42.998+09:00 |
| Closed |  |
| Head SHA | f7165a0a7e67c17cb8b9e49769ad93c9cf7c27cd |
| Migration Classification | github-source-branch |
| GitHub Source Branch | True |
| GitHub Recovered Branch | False |
| Diff Archive | [../diffs/MR-0021.diff](../diffs/MR-0021.diff) |

## Description

## 요약
이전 MR들에 달린 팀원 리뷰 피드백을 일괄 반영하고,
정적 분석 검수 과정에서 발견한 아키텍처 위반을 수정합니다.

## 작업 배경
- 관련 이슈: #S14P31A404-136
- 참고: MR !17~!21에 달린 리뷰 피드백 (@hh001204 )

## 주요 변경 사항

### .gitignore 모노레포 책임 분리 (리뷰 반영)
- 루트 `.gitignore`는 OS/IDE/Security 전역 규칙만 유지
- Android, Spring, Python 전용 규칙을 각각 `mobile/`, `backend/`, `ai/` 하위 `.gitignore`로 이관
- `!**/gradle/wrapper/gradle-wrapper.jar` 경로 명확화

### 아키텍처 위반 제거
- `core/network/NetworkModule.kt` (구버전, Manual Singleton) 삭제
  - `core → feature` 역방향 의존성의 근원이었음
  - Hilt 기반 `core/network/di/NetworkModule.kt`로 완전 통일
- `core/database/MobileDatabase.kt` 삭제
  - `core`가 `feature/sample`의 DAO를 직접 참조하는 역방향 의존성 해소
  - `feature/sample/data/local/db/SampleDatabase.kt`로 이동

### 도메인 계약 위치 교정
- `TranslateRepository`를 `core/network/` → `core/domain/`으로 이동
  - 네트워크 구현이 아닌 도메인 계약임을 명확히 표현

### Base URL 환경별 분리 (리뷰 반영)
- `app/build.gradle.kts`에서 루트 `.env`를 읽어 `BuildConfig.BASE_URL` 주입
- `NetworkModule`의 `localhost` 하드코딩 완전 제거
- `.env.example`에 `MOBILE_API_BASE_URL` 항목 추가
  - 에뮬레이터용 기본값: `http://10.0.2.2:8080/api/`

### 공통 UI 뼈대 추가
- `core/ui/components/`: AppPrimaryButton, AppSecondaryButton, AppTextField, AppDialog, AppLoadingIndicator (5종)
- `core/ui/feedback/`: AppErrorText, AppEmptyState, AppOfflineBanner (3종, 에러 관련 분리)
- `core/permission/`: PermissionGuide, PermissionRequestState (2종, COMM_002 대응)

### A/B 파트 소통 계약 완비
- `core/vision/SignRecognitionEngine.kt` 인터페이스 신규 작성
  - A파트가 B파트에 제공하는 유일한 소통 창구 확정

## 테스트 결과
- [x] Detekt / Ktlint 무결성 100% 통과 (core 코드 기준)
- [x] 역방향 의존성(core → feature) 0건 확인
- [x] 하드코딩 URL / 민감 정보 0건 확인

## 작성자 체크리스트
- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [x] 관련 이슈 S14P31A404-136 상태 업데이트

## 메모
이 MR은 1~5차 MR에 달린 모든 피드백을 Fix-Forward 전략으로 한 번에 반영한 브랜치입니다.
본 MR 머지 후 모바일 도메인은 피처 개발(A파트: 수어 인식, B파트: 통역 UI)을 시작할 수 있습니다. :grin: 

Closes S14P31A404-136

## Notes

### soshyzx123 at 2026-04-25T21:27:52.971+09:00 (system)

```text
requested review from @soshyzx123
```

### soshyzx123 at 2026-04-25T21:27:52.996+09:00 (system)

```text
assigned to @soshyzx123
```

### soshyzx123 at 2026-04-25T21:28:25.992+09:00 (system)

```text
requested review from @hh001204 and removed review request for @soshyzx123
```

### hh001204 at 2026-04-27T09:43:09.514+09:00 (system)

```text
deleted the `feature/S14P31A404-71-ui-theme-formatter` branch. This merge request now targets the `develop` branch
```

### hh001204 at 2026-04-27T09:48:35.418+09:00 (comment)

```text
전반적으로 변경 방향이 좋습니다.

특히 A/B 파트가 이후 병렬로 작업할 수 있도록 SignRecognitionEngine, TranslateRepository 같은 인터페이스를 먼저 잡아둔 부분이 유용해 보입니다.

현재 단계에서는 충분히 잘 정리된 변경이라고 생각합니다. 주말 동안 고생 많으셨습니다. 👍👍👍
```

### hh001204 at 2026-04-27T09:48:35.889+09:00 (system)

```text
approved this merge request
```

### soshyzx123 at 2026-04-27T09:58:48.200+09:00 (comment)

```text
감사합니다. 주말동안 검토해주시느라 고생 많으셨습니다ㅏㅏㅏㅏ :grin:
```

### hh001204 at 2026-04-27T10:42:46.103+09:00 (system)

```text
added 1 commit

<ul><li>f7165a0a - chore: ktlint 적용</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/21/diffs?diff_id=2008321&start_sha=9796f955063cc2939c4142a46d9959cbc3045f7d)
```

### hh001204 at 2026-04-27T10:47:42.999+09:00 (system)

```text
mentioned in commit 453319c9e73e670d1dcd9b8213c8a73499db3893
```


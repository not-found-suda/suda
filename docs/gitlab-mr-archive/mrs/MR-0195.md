# GitLab MR !195 - feat: FastAPI PyTorch 수어 추론 구현

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/195 |
| State | merged |
| Source | feature/S14P31A404-416-fastapi-pytorch-inference |
| Target | develop |
| Author | nej0720 |
| Assignees | nej0720 |
| Labels | ai, backend, feature |
| Created | 2026-05-18T14:22:05.756+09:00 |
| Updated | 2026-05-18T15:51:06.631+09:00 |
| Merged | 2026-05-18T15:51:02.312+09:00 |
| Closed |  |
| Head SHA | 325ee258b4000a07c8de8298972412e4e1279975 |
| Migration Classification | github-source-branch |
| GitHub Source Branch | True |
| GitHub Recovered Branch | True |
| Diff Archive | [../diffs/MR-0195.diff](../diffs/MR-0195.diff) |

## Description

## 요약

- FastAPI 수어 추론 서버에 V6 PyTorch TCN 모델 로딩 및 feature 추론 로직을 구현했습니다.

## 작업 배경

관련 이슈: S14P31A404-416\
참고 문서/기획:

## 주요 변경 사항

- `SIGN_MODEL_PATH` 기반 PyTorch 모델 로딩 구현
- `train_config_v6.json`, `label_map_v6.json` 기반 모델/config/label 로딩 구현
- flattened `30 * 332` feature를 `[1, 30, 332]`로 변환해 추론
- topK/confidence/margin/none class 응답 정책 구현
- Docker/환경변수/README에 모델 파일 mount 방식 반영

## 테스트 결과

- Python 3.11.9 / torch 2.5.1+cu121 환경에서 모델 로딩 성공
- CUDA 환경에서 `best_sign_model_v6.pt` 더미 feature 추론 성공
- `python -m py_compile ai/ai-server/app/main.py ai/ai-server/app/schemas.py ai/ai-server/app/model_service.py` 통과
- `docker compose config --quiet` 통과
- `git diff --check` 통과

## 작성자 체크리스트

- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [x] 로컬 빌드/런 정상
- [x] 관련 문서/주석/README 업데이트(필요 시)
- [ ] 이슈/Jira 상태 업데이트(In Review/Code Review)

## 메모

- `.pt` 모델 파일은 repo에 커밋하지 않고 `SIGN_MODEL_PATH` 또는 Docker volume mount로 주입합니다.
- 콘솔에서 한글 label이 깨져 보이는 현상은 출력 인코딩 문제이며, Python 내부 label map 로딩은 정상 유니코드로 확인했습니다.
- 실시간 smoothing/window 확정 정책은 모바일 또는 후속 정책 작업 범위로 분리했습니다.

Closes S14P31A404-416

## Notes

### nej0720 at 2026-05-18T14:22:05.902+09:00 (system)

```text
requested review from @hh001204
```

### nej0720 at 2026-05-18T14:22:08.949+09:00 (system)

```text
assigned to @nej0720
```

### nej0720 at 2026-05-18T14:23:49.509+09:00 (system)

```text
requested review from @nej0720 and removed review request for @hh001204
```

### nej0720 at 2026-05-18T14:23:55.520+09:00 (system)

```text
requested review from @milkswkim and removed review request for @nej0720
```

### milkswkim at 2026-05-18T14:54:52.035+09:00 (comment)

```text
여기서는 `/health`의 HTTP 성공 여부만 확인하고 있는데, 현재 `/health`는 모델 로딩 실패 시에도 200으로 `status: degraded`를 반환합니다. 그래서 `.pt` 누락이나 모델 로딩 실패 상태에서도 compose healthcheck가 통과할 수 있어 보입니다.

backend가 `condition: service_healthy`로 의존하고 있으니, healthcheck에서 `modelLoaded == true`까지 검사하거나 모델 로딩 실패 시 startup을 실패시키는 쪽이 안전할 것 같습니다.
```

### milkswkim at 2026-05-18T14:54:52.238+09:00 (comment)

```text
README는 `ai/ai-server`에서 실행하는 흐름처럼 보이는데, `.env.example`의 `SIGN_TRAIN_CONFIG_PATH=ai/models/...`, `SIGN_LABEL_MAP_PATH=ai/models/...`, `SIGN_MODEL_MODULE_PATH=ai/src/...`는 repo root 기준 경로입니다. 현재 구현은 상대 경로를 `Path.cwd()` 기준으로 해석하므로 실행 위치에 따라 파일을 찾지 못할 수 있어 보여요. 실행 위치 기준을 문서에 명확히 맞춰주면 좋겠습니다.
```

### milkswkim at 2026-05-18T14:54:52.472+09:00 (system)

```text
left review comments
```

### milkswkim at 2026-05-18T14:55:13.753+09:00 (comment)

```text
전체적으로 모델 로딩/추론 로직을 `SignModelService`로 분리하고, config/label map 검증 및 top-k 후보 반환까지 처리한 구조는 이해하기 좋았습니다.

정적 검증으로 JSON 파싱, Python compile, diff check는 통과 확인했습니다.
```

### nej0720 at 2026-05-18T15:06:24.820+09:00 (system)

```text
added 1 commit

<ul><li>f67809c4 - fix: AI 서버 healthcheck 및 경로 문서 보완</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/195/diffs?diff_id=2060226&start_sha=2060011397b7510481899b39d7563b4038ee1018)
```

### nej0720 at 2026-05-18T15:06:38.556+09:00 (comment)

```text
반영했습니다.

기존 `/health`는 모델 로딩 실패 시에도 HTTP 200을 반환해서 Compose healthcheck가 통과할 수 있었습니다.\
이제 모델 로딩 성공 시에만 `/health`가 HTTP 200과 `modelLoaded=true`를 반환하고, 모델 누락/로딩 실패 시에는 HTTP 503과 `modelLoaded=false`를 반환하도록 수정했습니다.

또한 Compose healthcheck도 단순 HTTP 성공 여부가 아니라 응답 JSON의 `modelLoaded == true`까지 검사하도록 변경했습니다.

검증:

- 모델 로딩 성공 시 `/health` -\> 200, `modelLoaded=true`
- 모델 누락 시 `/health` -\> 503, `modelLoaded=false`
```

### nej0720 at 2026-05-18T15:06:49.953+09:00 (comment)

```text
반영했습니다.

README의 로컬 실행 위치를 `ai/ai-server`로 명확히 적고, 로컬 실행 시 `SIGN_TRAIN_CONFIG_PATH`, `SIGN_LABEL_MAP_PATH`, `SIGN_MODEL_MODULE_PATH`는 실행 위치에 영향을 받지 않도록 절대경로를 사용하도록 문서를 수정했습니다.

`.env.example`도 같은 기준으로 절대경로 placeholder를 사용하도록 정리했습니다.
```

### nej0720 at 2026-05-18T15:07:04.661+09:00 (comment)

```text
검토 감사합니다. 두 코멘트 모두 반영했습니다.

- `/health`가 모델 로딩 성공 시에만 HTTP 200을 반환하도록 변경
- 모델 로딩 실패/모델 파일 누락 시 `/health`는 HTTP 503 반환
- Compose healthcheck에서 `modelLoaded == true`까지 검사하도록 변경
- README에 로컬 실행 위치와 절대경로 사용 기준 명시
- `.env.example`의 로컬 모델/config/label/model.py 경로도 절대경로 placeholder로 정리
```

### nej0720 at 2026-05-18T15:16:12.572+09:00 (system)

```text
changed this line in [version 3 of the diff](/s14-final/S14P31A404/-/merge_requests/195/diffs?diff_id=2060283&start_sha=f67809c40c64a0ca366e099fcf1579afa43bcf9b#4e101542f3898b6e1b2497b96a3ee1c6b0ccc091_8_8)
```

### nej0720 at 2026-05-18T15:16:12.799+09:00 (system)

```text
added 1 commit

<ul><li>ef4716a6 - fix: AI 서버 healthcheck 및 경로 문서 보완</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/195/diffs?diff_id=2060283&start_sha=f67809c40c64a0ca366e099fcf1579afa43bcf9b)
```

### hh001204 at 2026-05-18T15:21:16.042+09:00 (comment)

```text
**P2: 모델 파일과 config/label 파일이 서로 다른 버전이 될 수 있습니다**

현재 `.pt` 모델은 `/models` 볼륨에서 주입되지만, `train_config_v6.json`, `label_map_v6.json`,` model.py`는 Docker image 안에 복사된 파일을 기본으로 사용하고 있습니다.
나중에 새 모델만 `/models`에 교체하면, 모델은 새 버전인데 config/label은 예전 image 버전인 상태가 될 수 있습니다.

모델, config, label map, model.py를 같은 artifact 묶음으로 보고 같은 경로에서 mount하거나, 기본 container path도 `/models/...` 기준으로 맞추는 편이 안전해 보입니다.
```

### hh001204 at 2026-05-18T15:21:16.275+09:00 (comment)

```text
**P4: confidence threshold env 값이 잘못되면 앱이 health 503도 못 주고 뜨지 않을 수 있습니다**

`SIGN_CONFIDENCE_THRESHOLD`를 `float(...)`로 바로 파싱하고 있어서, env 값이 잘못 들어가면 `SignModelService()` 생성 단계에서 앱 import 자체가 실패할 수 있습니다.
모델 로딩 실패처럼 health에서 `503`으로 보여주려면 threshold 파싱도 `load()` 안에서 처리하거나 예외를 `load_error`로 잡는 방식이 더 안전합니다.
```

### hh001204 at 2026-05-18T15:21:16.481+09:00 (comment)

```text
**P3: Docker build context 변경으로 기존 .dockerignore가 적용되지 않을 수 있습니다**

이번에 build context가 `./ai/ai-server`에서 `./ai`로 바뀌었습니다. 그런데 `.dockerignore`는 `ai/ai-server/.dockerignore`에 있어서, Docker가 context root인 `ai/.dockerignore`로 인식하지 못할 수 있습니다.

이러면 ai 폴더 전체가 build context로 전송되어 빌드가 느려지거나 불필요한 모델/문서 파일이 context에 포함될 수 있습니다. `ai/.dockerignore`로 옮기거나, 현재 context 기준 ignore 파일을 추가하는 게 좋겠습니다.
```

### hh001204 at 2026-05-18T15:21:16.789+09:00 (comment)

```text
전체적으로는 이번 수정 방향 좋습니다. 특히 `healthcheck`를 실제 모델 로딩 상태와 연결한 건 배포 안정성 측면에서 꽤 중요한 보완이여서 남은 건 “모델 파일 세트가 항상 같은 버전으로 묶이는가” 정도만 더 단단히 잡으면 될 것 같습니다.

고생하셨습니다. 순우님의 리뷰 내용은 모두 수정한 것 확인했습니다. 머지 승인하겠습니다!! 👍👍👍
```

### hh001204 at 2026-05-18T15:21:16.887+09:00 (system)

```text
approved this merge request
```

### nej0720 at 2026-05-18T15:39:43.600+09:00 (system)

```text
changed this line in [version 4 of the diff](/s14-final/S14P31A404/-/merge_requests/195/diffs?diff_id=2060391&start_sha=ef4716a6c72cece466d363f859873e12ddb74bb5#8e9eb2c10185ea1a62499dfcea4db1ae1a615e71_12_16)
```

### nej0720 at 2026-05-18T15:39:43.846+09:00 (system)

```text
changed this line in [version 4 of the diff](/s14-final/S14P31A404/-/merge_requests/195/diffs?diff_id=2060391&start_sha=ef4716a6c72cece466d363f859873e12ddb74bb5#3cb648b44a0b9e3d6294450841adf132376156ba_202_205)
```

### nej0720 at 2026-05-18T15:39:44.094+09:00 (system)

```text
added 1 commit

<ul><li>325ee258 - fix: AI 모델 artifact 구성 및 health 안정성 보완</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/195/diffs?diff_id=2060391&start_sha=ef4716a6c72cece466d363f859873e12ddb74bb5)
```

### nej0720 at 2026-05-18T15:40:10.661+09:00 (comment)

```text
반영했습니다.

모델 파일만 `/models`에서 주입하고 config/label/model.py는 image 안의 파일을 사용하는 구조를 제거했습니다.\
이제 Docker 실행 시 `best_sign_model_v6.pt`, `train_config_v6.json`, `label_map_v6.json`, `model.py`를 모두 같은 artifact 디렉터리에서 `/models`로 mount해서 읽도록 변경했습니다.

이를 위해 기본 container path도 아래처럼 `/models` 기준으로 맞췄습니다.

- `SIGN_MODEL_PATH=/models/best_sign_model_v6.pt`
- `SIGN_TRAIN_CONFIG_PATH=/models/train_config_v6.json`
- `SIGN_LABEL_MAP_PATH=/models/label_map_v6.json`
- `SIGN_MODEL_MODULE_PATH=/models/model.py`

Docker image에서는 config/label/model.py 복사를 제거했습니다!
```

### nej0720 at 2026-05-18T15:40:22.899+09:00 (comment)

```text
반영했습니다.

`SIGN_CONFIDENCE_THRESHOLD`를 `SignModelService()` 생성 시점에 바로 `float()`로 파싱하지 않도록 변경했습니다.\
이제 모델 `load()` 단계에서 threshold를 파싱하고, 값이 잘못된 경우 앱 import 자체는 유지한 채 `load_error`로 잡히도록 했습니다.

검증 결과 잘못된 threshold 값이 들어오면 `/health`가 HTTP 503과 `modelLoaded=false`를 반환하는 것까지 확인했습니다!
```

### nej0720 at 2026-05-18T15:40:48.537+09:00 (comment)

```text
반영했습니다.

Docker build context가 `./ai`로 변경된 점에 맞춰 context root 기준의 `ai/.dockerignore`를 추가했습니다.\
더 이상 적용되지 않는 `ai/ai-server/.dockerignore`는 삭제했습니다.

새 `.dockerignore`에는 Python cache, venv, 기존 `models/`, `src/`, `.pt/.pth` 파일 등을 제외하도록 설정했습니다!
```

### nej0720 at 2026-05-18T15:41:21.722+09:00 (comment)

```text
리뷰 감사합니다 \~!

![CatCatMemeGIF.gif](/uploads/379645f2401755a20b629fb2d3ceb5da/CatCatMemeGIF.gif){width=205 height=226}
```

### hh001204 at 2026-05-18T15:50:57.552+09:00 (system)

```text
resolved all threads
```

### hh001204 at 2026-05-18T15:51:02.273+09:00 (system)

```text
mentioned in commit 728c5e1525a646c2fd38b33ad4d620a37fec3415
```


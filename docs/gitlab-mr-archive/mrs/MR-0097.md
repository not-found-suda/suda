# GitLab MR !97 - feat: 퀴즈 문제별 학습 결과 저장 연동

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/97 |
| State | merged |
| Source | feature/S14P31A404-214-app-save-problem-result |
| Target | develop |
| Author | hh001204 |
| Assignees | hh001204 |
| Labels | feature, mobile |
| Created | 2026-05-07T12:42:07.760+09:00 |
| Updated | 2026-05-07T13:57:38.625+09:00 |
| Merged | 2026-05-07T13:57:33.731+09:00 |
| Closed |  |
| Head SHA | a52152f31d32cad1dcd6bd358a6ffa315ee08efd |
| Migration Classification | github-source-branch |
| GitHub Source Branch | True |
| GitHub Recovered Branch | False |
| Diff Archive | [../diffs/MR-0097.diff](../diffs/MR-0097.diff) |

## Description

## 요약
- 퀴즈 문제별 답변 저장 API를 연동하고 저장/재시도/세션 종료 상태를 처리했습니다.


## 작업 배경
- 관련 이슈: #S14P31A404-214
- 참고 문서/기획: 학습 API 명세서 by 김민선

## 주요 변경 사항
- 퀴즈 세션 생성, 현재 문제 조회, 답변 저장, 세션 종료 API 연동
- STT 결과 기반 로컬 별점/재시도 후 최종 답변 저장 흐름 구현
- 답변 저장 실패, 세션 종료 실패, 다음 문제 조회 실패 재시도 상태 처리
- 카테고리/난이도 기준 단어 목록 응답 구조에 맞춰 로컬 채점 단어 매핑
- 스코프 아웃: 결과 상세 조회 화면 및 오프라인 큐 저장은 후속 작업에서 처리

## 스크린샷/로그
- API 연동 작업으로 별도 스크린샷 없음

##  테스트 결과 (선택사항)
- [x] 단위 테스트 작성 / 통과 여부
- [ ] API 응답/로그 캡처 (스크린샷 첨부)


## 작성자 체크리스트
- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [x] 로컬 빌드/런 정상
- [x] 관련 문서/주석/README 업데이트(필요 시)
- [x] 이슈/Jira 상태 업데이트(In Review/Code Review)

## 메모
- 서버에 문제 건너뛰기 API가 없어 퀴즈 화면의 skip 동작은 비활성화했습니다.


Closes S14P31A404-214

## Notes

### hh001204 at 2026-05-07T12:42:08.301+09:00 (system)

```text
requested review from @soshyzx123
```

### hh001204 at 2026-05-07T12:42:08.318+09:00 (system)

```text
assigned to @hh001204
```

### hh001204 at 2026-05-07T12:42:22.516+09:00 (system)

```text
changed the description
```

### hh001204 at 2026-05-07T12:44:15.900+09:00 (system)

```text
added 5 commits

<ul><li>3d43fea7...704dd86b - 4 commits from branch <code>develop</code></li><li>02415c23 - Merge branch &#39;develop&#39; into &#39;feature/S14P31A404-214-app-save-problem-result&#39;</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/97/diffs?diff_id=2030495&start_sha=3d43fea78b76ae437f36117b32a0e6435f711e6e)
```

### hh001204 at 2026-05-07T12:55:29.100+09:00 (system)

```text
added 1 commit

<ul><li>339a07e1 - fix: 퀴즈 결과 저장 검증 오류 수정</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/97/diffs?diff_id=2030566&start_sha=02415c237e8d291bb5dfec7003e71500ff4bdd52)
```

### hh001204 at 2026-05-07T13:09:36.960+09:00 (system)

```text
added 1 commit

<ul><li>6e86ede6 - fix: 퀴즈 이미지 표시와 세션 종료 재시도 처리 보완</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/97/diffs?diff_id=2030637&start_sha=339a07e13bded6342a5d49e68479f74f90b42d77)
```

### soshyzx123 at 2026-05-07T13:26:03.660+09:00 (comment)

```text
[P2] categoryId 누락을 기본 1번 카테고리로 대체하는 부분은 위험해 보입니다.

Screen.Quiz 라우트에서는 categoryId가 필수 인자인데, ViewModel에서는 누락 시 DEFAULT_CATEGORY_ID = 1L로 조용히 대체하고 있습니다.

라우팅 버그나 deep link 오류가 생겼을 때 잘못된 카테고리로 퀴즈 세션이 생성될 수 있으니, categoryId는 기본값을 두기보다 checkNotNull로 명확히 실패시키거나 Error state로 전환하는 편이 안전해 보입니다.

difficulty 기본값은 정책상 괜찮을 수 있지만, categoryId 기본값은 데이터 오염 위험이 있어 제거하는 게 좋아 보입니다. :grin:
```

### soshyzx123 at 2026-05-07T13:26:20.759+09:00 (comment)

```text
[P1] 랜덤 단어 목록으로 현재 문제 단어를 역매핑하는 구조가 불안정해 보입니다.

startSession()에서 getWords(categoryId, difficulty)로 단어 목록을 먼저 가져와 wordById를 만든 뒤, createSession()/getCurrentQuestion()의 wordId를 이 맵에서 찾고 있습니다.

그런데 현재 백엔드 getWords는 랜덤 5개를 반환하는 구조라, 퀴즈 세션이 고른 문제들과 사전에 조회한 단어 목록이 서로 달라질 수 있습니다. 이 경우 question.word가 빈 문자열이 되고, STT 저장 전에 “문제 단어 정보를 확인할 수 없습니다.” 상태로 막힐 수 있습니다.

current question 응답에 targetText/displayText를 포함하거나, wordId 상세 조회로 보강하거나, createSession 응답에서 실제 세션 단어 목록을 함께 내려주는 식으로 계약을 맞추는 게 필요해 보입니다. :asterisk:
```

### soshyzx123 at 2026-05-07T13:26:56.510+09:00 (comment)

```text
[P1] 현재 develop에 없는 퀴즈 API를 바로 호출하는 흐름이라 확인이 필요해 보입니다.

최신 origin/develop 백엔드에는 /v1/learn/quizzes/sessions 계열 컨트롤러가 아직 없는 것으로 보입니다. 그런데 이번 MR에서는 단어 목록에서 퀴즈 화면으로 진입하면 createSession API를 바로 호출하게 되어 있어, 이 MR만 먼저 머지되면 학습 시작 시 404로 실패할 수 있습니다.

백엔드 퀴즈 API MR이 선행/동시 머지되는 전제라면 MR 메모에 명시하면 좋겠고, 아니라면 당장은 “준비 중” 처리나 feature flag 등으로 실제 호출을 막는 편이 안전해 보입니다. :smiley: :smiley:
```

### soshyzx123 at 2026-05-07T13:30:41.314+09:00 (comment)

```text
전체적인 구조는 매우 좋아 보입니다. :grin:  

Repository/API/ViewModel/UI 상태가 분리되어 있고, 퀴즈 결과 저장 흐름을 미리 잡아둔 점도 후속 API 연동에 유효할 것 같습니다. :smiley: :smiley: 

다만 백엔드 API가 아직 없는 상태를 전제로 한 선구현이라면, MR 설명이나 코드 주석에 “백엔드 퀴즈 API 연동 전 임시 구현”이라는 점을 명확히 남겨두면 좋겠습니다. 특히 현재는 /v1/learn/quizzes/sessions 계열 API가 develop 기준으로 없어서, 이 MR만 머지되면 학습 시작 시 실제 호출이 404로 실패할 수 있습니다.

추가로 단어 목록 응답 wrapper({ words: [...] }) 기대 형태와 현재 develop의 List 직접 반환 형태가 다르고, getWords가 랜덤 단어를 반환하는 구조라 퀴즈 세션의 wordId와 사전 조회한 wordById가 불일치할 가능성이 있습니다. 이 부분은 백엔드 API 확정 시 current question 응답에 targetText/displayText를 포함하거나, 세션 생성 응답에서 실제 문제 단어 정보를 함께 내려주는 방향으로 계약을 맞추면 좋겠습니다.

->> 이 부분은 저도 마찬가지여서, 민선님에게 수정작업을 요청해서 다음 MR에서 수정될 것으로 보입니다. 참고만 부탁드립니다! :thumbsup_tone3: 


즉, “백엔드 미구현 전제의 UI/클라이언트 흐름 선구현”으로는 괜찮아 보이고, 머지 전에는 위 제약을 MR 메모에 명시하거나 학습 시작 버튼을 준비 중 처리할지 정도만 정하셔서 진행해주면 될거같습니다.
```

### soshyzx123 at 2026-05-07T13:31:06.143+09:00 (system)

```text
left review comments
```

### hh001204 at 2026-05-07T13:47:18.636+09:00 (comment)

```text
반영했습니다. Quiz 라우트의 `categoryId`는 필수 인자라 기본 1번 카테고리로 대체하지 않도록 `DEFAULT_CATEGORY_ID`를 제거했습니다. 이제 `savedStateHandle`에 `categoryId`가 없으면 `checkNotNull`로 명확히 실패하도록 변경했습니다.
```

### hh001204 at 2026-05-07T13:47:51.715+09:00 (comment)

```text
일부 반영했습니다. 앱에서는 `current question` 응답에 `targetText/word/displayText`가 포함될 경우 이를 우선 사용하도록 DTO와 도메인 모델을 확장했습니다. 해당 값이 없을 때만 기존 `wordId` 매핑을 fallback으로 사용합니다.

다만 `getWords`가 랜덤 5개를 반환하는 구조라 앱 단독으로는 완전 해결이 어렵습니다. 최종적으로는 `GET current question` 응답에 `targetText` 또는 word를 포함하도록 BE 계약을 맞추는 것이 필요합니다.
```

### hh001204 at 2026-05-07T13:48:54.862+09:00 (comment)

```text
확인했습니다. 현재 MR은 퀴즈 API 실연동을 전제로 진행했습니다. 

BE 퀴즈 API 머지 지연 시 호출 차단/준비 중 처리는 언젠가 별도로 반영하겠습니다.
```

### soshyzx123 at 2026-05-07T13:49:59.005+09:00 (system)

```text
resolved all threads
```

### hh001204 at 2026-05-07T13:50:09.540+09:00 (system)

```text
added 1 commit

<ul><li>a52152f3 - fix: 퀴즈 라우트 인자와 문제 단어 매핑 보강</li></ul>

[Compare with previous version](/s14-final/S14P31A404/-/merge_requests/97/diffs?diff_id=2030889&start_sha=6e86ede6bce0b73c0b5ca88c7c2fe80af8594c1c)
```

### soshyzx123 at 2026-05-07T13:51:17.651+09:00 (system)

```text
resolved all threads
```

### soshyzx123 at 2026-05-07T13:51:17.930+09:00 (system)

```text
approved this merge request
```

### soshyzx123 at 2026-05-07T13:57:33.970+09:00 (system)

```text
mentioned in commit 77fd81956eadb2c0e6b33d32d2d93e10fd0f5806
```


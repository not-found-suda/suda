# GitLab MR !19 - feat: Jetpack Compose Navigation 라우팅 뼈대 세팅

| Field | Value |
| --- | --- |
| GitLab URL | https://lab.ssafy.com/s14-final/S14P31A404/-/merge_requests/19 |
| State | merged |
| Source | feature/S14P31A404-50-navigation-setup |
| Target | develop |
| Author | soshyzx123 |
| Assignees | soshyzx123 |
| Labels | feature, mobile |
| Created | 2026-04-25T17:30:04.741+09:00 |
| Updated | 2026-04-25T18:26:29.886+09:00 |
| Merged | 2026-04-25T18:26:24.481+09:00 |
| Closed |  |
| Head SHA | f61370cd920dd8c56f643ab66d366bfd3e33ce20 |
| Migration Classification | metadata-or-patch-only |
| GitHub Source Branch | False |
| GitHub Recovered Branch | False |
| Diff Archive | [../diffs/MR-0019.diff](../diffs/MR-0019.diff) |

## Description

## 요약
- 앱 내 화면 간 전환을 위한 Jetpack Compose Navigation 라우팅 뼈대 및 기본 화면 틀 구축

## 작업 배경
- 관련 이슈: #S14P31A404-50
- 참고 문서/기획: SUDA 안드로이드 아키텍처 (Navigation + Hilt 연동)

## 주요 변경 사항
- `libs.versions.toml` 및 `build.gradle.kts`에 Compose Navigation 및 Hilt Navigation 연동 의존성 추가
- 오타로 인한 크래시 방지를 위해 화면 경로(Route)를 상수로 관리하는 `Screen` 봉인 클래스(Sealed Class) 작성
- 최상위 화면 라우팅을 담당하는 `MobileNavHost.kt` 작성 (현재 껍데기뿐인 Conversation, Sign 화면 연결)
- `MainActivity.kt`에 `rememberNavController()` 부착 및 진입점을 `MobileNavHost`로 변경

## 스크린샷/로그
- 

## 테스트 결과 (선택사항)
- [x] 단위 테스트 작성 / 통과 여부 - Ktlint 코드 컨벤션 검사 통과 확인

## 작성자 체크리스트
- [x] Secret/Env/개인정보가 포함되지 않음
- [x] 불필요한 로그/디버그 코드 제거
- [x] 로컬 빌드/런 정상
- [x] 관련 문서/주석/README 업데이트(필요 시)
- [x] 이슈/Jira 상태 업데이트(In Review/Code Review)

## 메모
- **리뷰어 확인 사항**: 앞으로 새로운 화면(Screen)이 추가될 때마다 `Screen.kt`에 라우트를 정의하고 `MobileNavHost`에 `composable` 블록을 추가하시면 됩니다. 각 화면별 뷰모델은 `hiltViewModel()`을 사용해 주입하면 됩니다.

Closes S14P31A404-50

## Notes

### soshyzx123 at 2026-04-25T17:30:04.904+09:00 (system)

```text
requested review from @hh001204
```

### soshyzx123 at 2026-04-25T17:30:05.086+09:00 (system)

```text
assigned to @soshyzx123
```

### hh001204 at 2026-04-25T18:21:00.233+09:00 (system)

```text
deleted the `feature/S14P31A404-62-network-setup` branch. This merge request now targets the `develop` branch
```

### hh001204 at 2026-04-25T18:25:28.523+09:00 (comment)

```text
전반적으로 Jetpack Compose Navigation 라우팅 뼈대는 잘 정리되어 있다고 생각합니다. `Screen`으로 route를 모아두고 `MobileNavHost`에서 진입점을 관리하는 구조도 이후 화면이 늘어날 때 확장하기 좋아 보입니다.

다만 현재 CI 기준으로는 `MobileNavHost.kt`의 `TODO` 주석이 detekt의 `ForbiddenComment` 규칙에 걸릴 수 있어, 해당 부분만 정리하면 좋을 것 같습니다.

그리고 `MobileNavHost` 같은 `@Composable` 함수명은 대문자로 시작하는 것이 자연스러운데, 현재 ktlint에서는 일반 함수명 규칙으로 판단해 실패할 수 있습니다. 이 부분은 파일마다 `@file:Suppress`를 추가하기보다는 @hh001204가 별도 커밋으로 루트 `.editorconfig`에 `ktlint_function_naming_ignore_when_annotated_with = Composable` 설정을 반영해두겠습니다.

고생하셨습니다! 👍
```

### hh001204 at 2026-04-25T18:25:28.710+09:00 (system)

```text
approved this merge request
```

### hh001204 at 2026-04-25T18:26:24.601+09:00 (system)

```text
mentioned in commit c68a0ae4a34d7ebc73a638f82ae05039f3f1f708
```


diff --git a/.gitlab/merge_request_templates/Default.md b/.gitlab/merge_request_templates/Default.md
--- a/.gitlab/merge_request_templates/Default.md
+++ b/.gitlab/merge_request_templates/Default.md
@@ -3,7 +3,7 @@
 
 
 ## 작업 배경
-- 관련 이슈: Closes #
+- 관련 이슈: #
 - 참고 문서/기획:
 
 ## 주요 변경 사항


diff --git a/.env.example b/.env.example
--- a/.env.example
+++ b/.env.example
@@ -4,7 +4,7 @@ POSTGRES_PORT=5432
 
 # Backend runtime
 SPRING_PROFILES_ACTIVE=dev
-DB_URL=jdbc:postgresql://db:5432/appdb
+DB_URL=jdbc:postgresql://localhost:5432/appdb
 DB_USERNAME=appuser
 DB_PASSWORD=apppassword
 AUTH_JWT_SECRET=change-this-dev-jwt-secret-at-least-32-bytes
@@ -16,7 +16,7 @@ AUTH_REFRESH_COOKIE_SECURE=false
 AUTH_REFRESH_COOKIE_SAME_SITE=Lax
 AUTH_REFRESH_COOKIE_PATH=/
 SECURITY_CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:5173,http://127.0.0.1:5173
-REDIS_HOST=redis
+REDIS_HOST=localhost
 REDIS_PORT=6379
 REDIS_PASSWORD=redispassword
 


diff --git a/.gitlab-ci.yml b/.gitlab-ci.yml
--- a/.gitlab-ci.yml
+++ b/.gitlab-ci.yml
@@ -11,9 +11,9 @@ stages:
 default:
   interruptible: true
 
-.mr_to_dev_or_main:
+.mr_to_develop_or_main:
   rules:
-    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "dev" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
+    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
 
 .mr_to_main_only:
   rules:
@@ -25,8 +25,8 @@ main_source_guard:
   extends: .mr_to_main_only
   script:
     - |
-      if [ "$CI_MERGE_REQUEST_SOURCE_BRANCH_NAME" != "dev" ]; then
-        echo "Only dev -> main merge requests are allowed."
+      if [ "$CI_MERGE_REQUEST_SOURCE_BRANCH_NAME" != "develop" ]; then
+        echo "Only develop -> main merge requests are allowed."
         exit 1
       fi
       echo "Branch flow check passed: $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME -> $CI_MERGE_REQUEST_TARGET_BRANCH_NAME"
@@ -35,7 +35,7 @@ backend-check:
   stage: verify
   image: eclipse-temurin:21-jdk
   rules:
-    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "dev" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
+    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
       changes:
         - backend/**/*
         - .gitlab-ci.yml
@@ -60,7 +60,7 @@ frontend-check:
   stage: verify
   image: node:24
   rules:
-    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "dev" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
+    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
       changes:
         - frontend/**/*
         - .gitlab-ci.yml
@@ -84,6 +84,6 @@ secret-scan:
   image:
     name: ghcr.io/gitleaks/gitleaks:v8.30.0
     entrypoint: [""]
-  extends: .mr_to_dev_or_main
+  extends: .mr_to_develop_or_main
   script:
     - gitleaks detect --source . --no-git --redact --exit-code 1


diff --git a/docker-compose.yml b/docker-compose.yml
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -3,7 +3,7 @@ name: s14p31a404
 services:
   db:
     image: postgres:16-alpine
-    container_name: postgresql
+    container_name: db
     environment:
       TZ: Asia/Seoul
       PGTZ: Asia/Seoul



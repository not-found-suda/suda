diff --git a/mobile/gradle.properties b/mobile/gradle.properties
--- a/mobile/gradle.properties
+++ b/mobile/gradle.properties
@@ -15,4 +15,5 @@ org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
 kotlin.code.style=official
 android.useAndroidX=true
 android.nonTransitiveRClass=true
+org.gradle.caching=true
 org.gradle.configuration-cache=true


diff --git a/.gitlab-ci.yml b/.gitlab-ci.yml
--- a/.gitlab-ci.yml
+++ b/.gitlab-ci.yml
@@ -182,20 +182,23 @@ backend-flyway-check:
 
 mobile-check:
   stage: verify
-  image: eclipse-temurin:21-jdk
+  image: ghcr.io/cirruslabs/android-sdk:35
   rules:
     - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && ($CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "develop" || $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main")'
       changes:
         - mobile/**/*
         - .gitlab-ci.yml
+      variables:
+        MOBILE_GRADLE_TASKS: "ktlintCheck detekt testDebugUnitTest lintDebug"
+    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
+      changes:
+        - mobile/**/*
+        - .gitlab-ci.yml
+      variables:
+        MOBILE_GRADLE_TASKS: "ktlintCheck detekt lintDebug testDebugUnitTest assembleDebug"
     - when: never
   variables:
     GRADLE_USER_HOME: "$CI_PROJECT_DIR/.gradle"
-    ANDROID_HOME: "$CI_PROJECT_DIR/android-sdk"
-    ANDROID_SDK_ROOT: "$CI_PROJECT_DIR/android-sdk"
-    ANDROID_COMPILE_SDK: "35"
-    ANDROID_BUILD_TOOLS: "36.0.0"
-    ANDROID_CMDLINE_TOOLS_REV: "13114758"
   cache:
     - key:
         prefix: mobile-gradle
@@ -207,17 +210,13 @@ mobile-check:
       paths:
         - .gradle/caches
         - .gradle/wrapper
-    - key: "android-sdk-${ANDROID_COMPILE_SDK}-${ANDROID_BUILD_TOOLS}-${ANDROID_CMDLINE_TOOLS_REV}"
-      paths:
-        - android-sdk
+        - mobile/.gradle/configuration-cache
   before_script:
-    - chmod +x .gitlab/scripts/setup-android-sdk.sh
-    - ./.gitlab/scripts/setup-android-sdk.sh
     - cd mobile
     - chmod +x gradlew
   script:
-    - echo "[mobile-check] running quality gate (ktlint/detekt/lint/test/assemble)"
-    - ./gradlew ktlintCheck detekt lintDebug testDebugUnitTest assembleDebug --no-daemon --console=plain --warning-mode=summary
+    - echo "[mobile-check] running quality gate (${MOBILE_GRADLE_TASKS})"
+    - ./gradlew ${MOBILE_GRADLE_TASKS} --build-cache --no-daemon --console=plain --warning-mode=summary
 
 secret-scan:
   stage: security


